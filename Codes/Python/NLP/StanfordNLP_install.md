### Install StanfordNLP package through Conda

- create a new environment
    conda create -n StanfordNLP python=3.6
- install StanfordNLP through `conda`
    conda install -c conda-forge stanfordcorenlp
